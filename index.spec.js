describe('index', () => {

  it('should throw when version not valid', async () => {

  });

  it('should not publish when dry run mode enabled', async () => {

  });

  it('should tag and push images', async () => {

  });

  it('should tag and push pre-release images', async () => {

  });

  it('should use suffix', async () => {

  });

  it('should use target image when provided', async () => {

  });

  it('should only publish channel tag when --channelOnly is true', async () => {

  });

});
